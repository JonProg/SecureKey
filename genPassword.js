export function createPassword(checkers, size, output){
    let password = '';
    for(checker.checked === true of checkers){
        if(checker.id === "upper-letter"){

        }

        if(checker.id === "down-letter"){

        }

        if(checker.id === "numbers"){

        }

        if(checker.id === "simboly"){

        }
    }
}